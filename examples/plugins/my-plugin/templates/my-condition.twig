{% import "_includes/forms" as forms %}

{{
    forms.textField({
        label: 'My variable'|t('my-plugin'),
        name: 'myVariable',
        required: true,
        value: condition.myVariable,
        errors: condition.getErrors('myVariable')
    })
}}